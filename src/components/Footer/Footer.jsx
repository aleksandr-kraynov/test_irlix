import './Footer.scss';
import logo from '../Logo.svg';

const Footer = () => {    
    return (
        <footer>
            <img src={ logo }/>
        </footer>
    );    
}

export default Footer;