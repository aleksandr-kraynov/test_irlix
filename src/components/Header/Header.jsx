import './Header.scss';
import logo from '../Logo.svg';

const Header = () => {    
    return (            
        <header>
            <img src={ logo } />
        </header>             
    );   
}

export default Header;